spring:
  application:
    name: order-service
    
#server:
#  port: 8201
#注册中心
eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
#启用hystrix降级熔断
feign:  hystrix:    enabled: true
#暴露指定的监控端点
management:  endpoints:    web:      exposure:        include:        - hystrix.stream
#配置order集群
---
spring:  profiles: order1
server:  port: 8201---
spring:  profiles: order2
server:  port: 8202